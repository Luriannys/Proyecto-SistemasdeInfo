import { Group } from "./Group";
class Testimony{
    constructor(date){
        Float(this.Rating);
        this.date;
        this.comment;
    }
    Write(Text,TGroup){
        let Textstring="";
        Textstring=(Textstring+Text);
    }
    Rate(Value,TGroup){
        TGroup.Prate=((TGroup.Prate+Value)/2);
    }
}